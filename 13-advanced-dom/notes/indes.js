
const add = (a,b) => a + b
const sub = (a,b) => a - b
const mul = (a,b) => a * b
const div = (a,b) => a / b
const mod = (a,b) => a % b


const doCalculation = (calculation) => calculation(a,b)


// calculat

// const doCalculation = () => (
	
// )

// // get operator btns
// var operatorBtns = document.querySelectorAll(".operator")
// // add event listeners to click
// for(var i=0;i<operatorBtns.length;i++) {
// 	operatorBtns[i].addEventListener("click", doCalculation)
// }


// // prompt for inputs
// var num1 = document.getElementById("num1")
// var num2 = document.getElementById("num2")
// var operation = prompt("Operation: add, sub, mul, div, exp")

// // print results
// console.log(calculator())